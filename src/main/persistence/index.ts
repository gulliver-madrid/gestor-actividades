import { guardarActividades, leerActividades } from './activities';
import { saveUsers, readUsers } from './users';
import { guardarProyectos, readProjects, saveProjectsObject } from './projects';

export default {
    guardarActividades,
    leerActividades,
    saveUsers,
    readUsers,
    readProjects,
    guardarProyectos,
    saveProjectsObject,
};
